JUJU App代码审查完成
状态: 通过
安全评分: 90/100
发现: 31个问题（0严重/15中等/16低风险）
详情: ~/.hermes/workspace/juju-platform-all/reviews/code-review-2026-05-03-v5.md
关键修复:
- authLimiter收紧至3次（原10次）
- 验证码改用crypto.randomInt（原Math.random）
- 订单权限校验全面添加（getOrderById/getOrderByOrderNo/getOrderList/getOrderTickets/getOrderRefund/updateOrderStatus/generateTickets/auditRefund/getUserOrders）
- Token黑名单机制实现（Redis-backed）
- ui-themes.js tag参数白名单校验（仅字母数字中文，长度1-20）
- bankCardService使用随机IV（原固定IV）
- CORS credentials语法修复（=== 'true'）
- 测试Token默认ID改为9999，生产环境绝对禁止
待处理:
- mockVerifyCodes需从内存迁移至Redis（生产环境必需）
- RN Token存储需从AsyncStorage迁移至Keychain/Keystore
- APK需重建以包含App.tsx BackHandler修复
- 明文密码迁移通道待数据库全部bcrypt后移除
- /tickets/code/:code和/number/:ticketNo公开路由需评估业务必要性
新发现:
- RN AsyncStorage存储Token（非加密）- 建议迁移
- RN AsyncStorage存储refreshToken - 建议迁移
- RN AsyncStorage缓存敏感数据 - 建议加密
审查陷阱记录:
- read_file/terminal工具会将===显示为***，必须用Python open('rb')+hex()验证真实内容
- ES6简写{display}在原始字节中显示为不完整语法，实则为合法简写
- 历史报告中的问题描述可能与实际代码不符，必须实际读取文件验证
