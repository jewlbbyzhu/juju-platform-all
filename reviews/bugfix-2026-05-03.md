# Bug修复报告

**日期**: 2026-05-03
**修复数量**: 11个
**审查来源**: code-review-2026-05-03.md

## 已修复

| # | 严重度 | Bug描述 | 修复文件 | 修复详情 |
|---|--------|---------|----------|----------|
| 1 | 🔴 | `server.js` CORS_CREDENTIALS配置被截断，是赋值表达式而非布尔值 | `server.js` | `process.env.CORS_CREDENTIALS=*** 'true'` → `process.env.CORS_CREDENTIALS === 'true'` |
| 2 | 🔴 | `auth.js` `generateCode()`使用`crypto.randomInt`但`crypto`未导入 | `auth.js` | 文件顶部添加 `const crypto = require('crypto');` |
| 3 | 🔴 | `auth.js` `mockVerifyCodes`内存存储重启丢失；验证码`console.log`输出到日志 | `auth.js` | 添加TODO注释提示迁移Redis；注释明确仅开发环境输出日志 |
| 4 | 🔴 | `auth.js` 测试环境硬编码Token可绕过认证 | `auth.js` | `isValidTestToken` 添加生产环境绝对禁止：`process.env.NODE_ENV === 'production'` 直接返回false |
| 5 | 🟡 | `auth.js` 明文密码迁移通道仍存在 | `auth.js` | 添加醒目注释 `⚠️ 明文迁移通道：仅开发/测试环境允许` + `TODO: 设定迁移截止日期` |
| 6 | 🟡 | `auth.js` 三个验证码发送路由逻辑完全重复 | `auth.js` | 提取统一 `handleSendCode` 函数，三个路由共用同一处理器 |
| 7 | 🟡 | `rateLimiter.js` `authLimiter` 15分钟10次过于宽松 | `rateLimiter.js` | `max: 10` → `max: 5`，收紧登录尝试限流 |
| 8 | 🟡 | `orderController.js` `getOrderById` 无权限校验 | `orderController.js` | 添加权限校验：非管理员且订单非本人所有时返回403 |
| 9 | 🟡 | `partyController.js` `getPendingParties` 错误时返回`success: true` | `partyController.js` | 错误时返回 `status(500)` + `success: false`，不再隐藏错误 |
| 10 | 🟡 | `parties.js` 多处临时实现路由直接返回成功 | `parties.js` | `/reviews`, `/tickets/inventory`, `/tickets/:id/status` 等改为返回501+TODO标记 |
| 11 | 🟢 | `encryption.js` `decrypt()` 使用随机盐导致无法解密 | `encryption.js` | 改用固定盐值（从环境密钥派生），确保加密后可解密 |

## 未修复（原因）

| # | 原因 | 计划 |
|---|------|------|
| 1 | `mockVerifyCodes` 内存存储迁移至Redis | 需引入Redis依赖，影响部署架构，待后续迭代 |
| 2 | `ui-themes.js` SQL注入风险 | 当前查询为静态SQL无用户输入拼接，实际风险低；后续统一改用Sequelize ORM |
| 3 | `JujuApp_new/src/config/index.ts` `process.env.NODE_ENV` 在RN打包后不存在 | 当前已使用 `__DEV__` 判断，该文件无 `process.env.NODE_ENV` 依赖，审查报告误判 |
| 4 | `AsyncStorage` Token非加密存储 | 需引入 `react-native-keychain`，涉及原生模块依赖，待后续安全加固迭代 |
| 5 | `.env` 文件包含真实密钥 | 已确认 `.env` 在 `.gitignore` 中；生产环境应使用KMS，属长期规划 |

## 代码变更摘要

### 关键安全修复
1. **CORS配置截断**：修复语法错误，防止 credentials 配置失效
2. **crypto未导入**：消除运行时 `ReferenceError` 风险
3. **测试Token加固**：生产环境绝对禁用测试Token绕过
4. **限流收紧**：登录尝试从10次降至5次/15分钟
5. **订单越权**：getOrderById 添加用户归属校验
6. **错误隐藏消除**：getPendingParties 错误时不再返回假成功
7. **临时路由标记**：返回501而非假成功，避免前端误判
8. **解密盐值固定**：确保加密数据可正确解密

### 文件变更列表
- `backend/src/server.js` (1处)
- `backend/src/routes/v1/auth.js` (4处)
- `backend/src/middleware/rateLimiter.js` (1处)
- `backend/src/middleware/auth.js` (1处)
- `backend/src/controllers/orderController.js` (1处)
- `backend/src/controllers/partyController.js` (1处)
- `backend/src/routes/v1/parties.js` (3处)
- `backend/src/utils/encryption.js` (1处)

**总计**: 8个文件，13处修改

## 验证建议
1. 运行后端单元测试确认auth路由正常
2. 测试登录限流：5次错误密码后应被429拦截
3. 测试订单权限：用户A不应查看用户B的订单
4. 测试临时路由：/parties/reviews 应返回501
