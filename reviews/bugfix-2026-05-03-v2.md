# Bug修复报告

**日期**: 2026-05-03
**修复数量**: 9个
**审查来源**: code-review-2026-05-03.md, ux-test-2026-05-03.md
**Git Commit**: 6ed47a56

## 已修复

| # | 严重度 | Bug描述 | 修复文件 | 修复详情 |
|---|--------|---------|----------|----------|
| 1 | 🔴 | `server.js` CORS_CREDENTIALS配置语法错误（赋值表达式而非布尔比较） | `server.js` | `process.env.CORS_CREDENTIALS === 'true'` 正确布尔比较 |
| 2 | 🔴 | `auth.js` `mockVerifyCodes`内存存储无清理机制，可能导致内存无限增长 | `auth.js` | 添加 `setInterval` 每10分钟清理过期验证码 |
| 3 | 🔴 | UX测试：登录页BACK键直接退出APP | `App.tsx` | 全局BackHandler拦截Login页返回键，提示"请先登录以继续使用" |
| 4 | 🟡 | `orderController.js` `getOrderByOrderNo` 无权限校验 | `orderController.js` | 添加权限校验：非管理员且订单非本人所有时返回403 |
| 5 | 🟡 | `orderController.js` `getOrderTickets` 无权限校验 | `orderController.js` | 添加权限校验：非管理员只能查看自己订单的票券 |
| 6 | 🟡 | `orderController.js` `getOrderRefund` 无权限校验 | `orderController.js` | 添加权限校验：非管理员只能查看自己订单的退款 |
| 7 | 🟡 | `orderController.js` `updateOrderStatus` 无权限校验 | `orderController.js` | 添加权限校验：仅管理员可更新订单状态 |
| 8 | 🟡 | `orderController.js` `generateTickets` 无权限校验 | `orderController.js` | 添加权限校验：仅管理员可生成票券 |
| 9 | 🟡 | `orderController.js` `auditRefund` 无权限校验 | `orderController.js` | 添加权限校验：仅管理员可审核退款 |
| 10 | 🟡 | `orderController.js` `getUserOrders` 无权限校验 | `orderController.js` | 添加权限校验：非管理员只能查看自己的订单 |

## 未修复（原因）

| # | 原因 | 计划 |
|---|------|------|
| 1 | `mockVerifyCodes` 内存存储迁移至Redis | 需引入Redis依赖，影响部署架构，待后续迭代（当前已加定时清理作为过渡方案） |
| 2 | `AsyncStorage` Token非加密存储 | 需引入 `react-native-keychain`，涉及原生模块依赖，待后续安全加固迭代 |
| 3 | `.env` 文件包含真实密钥 | 已确认 `.env` 在 `.gitignore` 中；生产环境应使用KMS，属长期规划 |
| 4 | 明文密码迁移通道 | 已添加 `NODE_ENV !== 'production'` + `ALLOW_LEGACY_PLAINTEXT === 'true'` 双重保护，待数据库全部迁移后移除 |

## 代码变更摘要

### 关键安全修复
1. **CORS配置修复**：`credentials` 从赋值表达式改为严格相等比较，确保布尔值正确解析
2. **验证码内存管理**：添加定时清理机制，防止开发/测试环境内存泄漏
3. **订单权限加固**：补充7处权限校验，覆盖订单查询、状态更新、票券生成、退款审核等敏感操作
4. **登录页UX优化**：拦截BACK键静默退出，改为提示用户必须登录

### 文件变更列表
- `backend/src/server.js` (1处)
- `backend/src/routes/v1/auth.js` (1处 - 验证码清理机制)
- `backend/src/controllers/orderController.js` (7处 - 权限校验)
- `JujuApp_new/App.tsx` (1处 - BACK键拦截)

**总计**: 4个文件，10处修改

## 验证建议
1. 运行后端单元测试确认auth路由正常
2. 测试订单权限：用户A不应查看用户B的订单/票券/退款
3. 测试管理员权限：普通用户不应能更新订单状态/生成票券/审核退款
4. 测试登录页BACK键：应提示"请先登录以继续使用"而非退出APP
5. 测试验证码过期：等待5分钟后验证码应自动失效并被清理
