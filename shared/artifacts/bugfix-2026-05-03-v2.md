# Bug修复报告

**日期**: 2026-05-03
**修复数量**: 4个（本次session新增）
**Git Commit**: 6edd5f15
**审查来源**: code-review-2026-05-03.md, ux-test-2026-05-03.md

## 已修复

| # | 严重度 | Bug描述 | 修复文件 | 修复详情 |
|---|--------|---------|----------|----------|
| 1 | 🔴 | `errorHandler.js` 非test环境返回堆栈，导致production也泄露 | `backend/src/utils/errorHandler.js` | `process.env.NODE_ENV !== 'test'` → `=== 'development'`，仅开发环境返回stack |
| 2 | 🔴 | `ui-themes.js` tag参数SQL校验不够严格，静默过滤非法字符 | `backend/src/routes/v1/ui-themes.js` | 改为严格白名单校验，非法字符直接返回400拒绝请求 |
| 3 | 🟡 | `tickets.js` `/code/:code` 和 `/number/:ticketNo` 公开路由注释不清 | `backend/src/routes/v1/tickets.js` | 更新注释明确为"公开验票接口，无需登录但需防枚举" |
| 4 | 🟡 | `socialController.js` 30处`console.error`直接输出错误对象 | `backend/src/controllers/socialController.js` | 全部改为`logger.error`结构化日志，仅记录`error.message` |

## 复查确认无需修复（审查误判修正）

| # | 文件 | 原审查结论 | 实际验证 |
|---|------|-----------|---------|
| 1 | `dataAdapter.js` | 返回`error.message`+`error.stack`给客户端 | ❌ **误判** — catch块仅调用`logger.error`记录服务端日志，返回给客户端的是`originalJson(data)`（原始响应数据），不包含任何错误信息 |
| 2 | `securityValidator.js` | 返回`error.message`+`error.stack`给客户端 | ❌ **误判** — 所有catch块返回给客户端的是通用错误消息（`'Validation system error'`/`'File validation system error'`），不包含stack或具体错误信息。`error.message`仅出现在服务端`logger.error`中 |

## 未修复（原因）

| # | 原因 | 计划 |
|---|------|------|
| 1 | `AsyncStorage` Token非加密存储 | 需引入`react-native-keychain`，涉及原生模块依赖，待后续安全加固迭代（P2） |
| 2 | `bankCardService.js` IV使用环境变量 | 审查结论为"需确认"，当前代码已使用环境变量IV，是否为固定值取决于运维配置。建议后续改为随机IV+随密文存储（P1） |
| 3 | 明文密码迁移通道 | 已添加`NODE_ENV !== 'production'` + `ALLOW_LEGACY_PLAINTEXT === 'true'`双重保护，待数据库全部迁移bcrypt后移除（P2） |
| 4 | 登录页BACK键直接退出APP | ⚠️ **代码已修复但APK未重建** — App.tsx已添加BackHandler拦截，但APK构建于2026-05-02 04:05，未包含最新修复。需重新构建APK |
| 5 | 用户协议链接无响应 | UX测试发现，待前端实现协议页面（P1） |

## 代码变更摘要

### 关键安全修复
1. **errorHandler堆栈泄露修复**: 消除production环境返回错误堆栈的重大安全隐患
2. **ui-themes tag白名单**: 从"静默过滤"改为"严格拒绝"，防止任何绕过尝试
3. **socialController结构化日志**: 30处`console.error`改为结构化logger，避免敏感信息通过日志系统泄露
4. **tickets公开路由文档化**: 明确公开验票接口的业务设计和安全考量

### 文件变更列表
- `backend/src/utils/errorHandler.js` (1处)
- `backend/src/routes/v1/ui-themes.js` (1处)
- `backend/src/routes/v1/tickets.js` (2处注释)
- `backend/src/controllers/socialController.js` (30处)

**总计**: 4个文件，34处修改

## 验证建议
1. 测试errorHandler: 设置`NODE_ENV=production`，确认错误响应不包含`stack`字段
2. 测试ui-themes tag校验: 发送`tag=<script>`应返回400而非200
3. 确认socialController无`console.error`残留: `grep -c "console.error" backend/src/controllers/socialController.js` 应为0
4. **重新构建APK**: 确保BackHandler修复被打包进JS bundle

## 与之前修复的关系

本次修复是2026-05-03代码审查的**第二轮修复**:
- 第一轮（bugfix-2026-05-03-v2.md）: 修复了10个问题（CORS、验证码清理、order权限、BackHandler代码）
- 第二轮（本次）: 修复4个新问题 + 修正2个审查误判

**累计2026-05-03修复**: 14个代码问题 + 2个审查误判修正
