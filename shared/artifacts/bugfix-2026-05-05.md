# Bug修复报告

**日期**: 2026-05-05
**修复数量**: 6个（4项安全问题）
**Git Commit**: `f9488960` — fix: bcrypt rounds 统一改为12 + Wallet defaultScope + Math.random验证码修复

---

## 已修复

| # | 严重度 | Bug描述 | 修复文件 | 修复详情 |
|---|--------|---------|----------|----------|
| 1 | 🔴 | **bcrypt rounds不一致** — adminService.js 3处使用10 | `backend/src/services/adminService.js` | `bcrypt.hash(..., 10)` → `bcrypt.hash(..., 12)` (line 148, 192, 423) |
| 2 | 🔴 | **bcrypt rounds不一致** — Wallet.js 2处使用10 | `backend/src/models/Wallet.js` | `bcrypt.hash(..., 10)` → `bcrypt.hash(..., 12)` (line 62, 67) |
| 3 | 🔴 | **Wallet.toJSON()暴露password字段** — walletController.js 直接返回含password哈希 | `backend/src/models/Wallet.js` | 添加 `defaultScope: { attributes: { exclude: ['password'] } }` |
| 4 | 🔴 | **bcrypt rounds不一致** — v2/auth.js 2处使用10 | `backend/src/routes/v2/auth.js` | `bcrypt.hash(..., 10)` → `bcrypt.hash(..., 12)` (line 151) |
| 5 | 🔴 | **Math.random()生成验证码可预测** — v2/auth.js captcha路由 | `backend/src/routes/v2/auth.js` | `Math.random().toString(10).slice(2,6)` → `crypto.randomInt(100000, 999999)` |
| 6 | 🔴 | **bcrypt rounds不一致** — walletService.js 1处使用10 | `backend/src/services/walletService.js` | `bcrypt.hash(..., 10)` → `bcrypt.hash(..., 12)` (line 297) |

---

## 未修复（testDataSeeder.js - 测试数据种子文件）

| # | 严重度 | 原因 | 计划 |
|---|--------|------|------|
| 1 | 🟢 | `testDataSeeder.js:46` 使用 `bcrypt.hash('admin123', 10)` — 测试数据种子文件，非生产代码 | 不修复（测试环境专用） |

---

## 验证结果

### bcrypt rounds 一致性验证
```bash
# 扫描结果：无 rounds=10 残留（testDataSeeder.js 除外）
grep -rn 'bcrypt.hash.*10' backend/src --include='*.js' | grep -v testDataSeeder
# 输出：无匹配 ✅
```

### crypto.randomInt 验证码验证
```bash
# 扫描结果：v2/auth.js 已修复，v1/auth.js 已在此前修复
grep -rn 'Math.random.*toString.*slice' backend/src --include='*.js'
# 输出：无匹配 ✅
```

### Wallet defaultScope 验证
```bash
grep -A2 'defaultScope' backend/src/models/Wallet.js
# 输出：defaultScope: { attributes: { exclude: ['password'] } } ✅
```

---

## 修复来源

- **审查报告**: `reviews/code-review-2026-05-05.md`
- **问题**: bcrypt rounds一致性（首次发现 adminService/Wallet/v2/auth.js 使用 rounds=10）
- **安全评分变化**: 88/100 → 预计恢复至 95/100

---

## 遗留未修复问题

| # | 严重度 | 问题 | 状态 |
|---|--------|------|------|
| 1 | 🟡 | `walletController.js` 多处 `wallet.toJSON()` — 已被 `defaultScope` 修复 | ✅ 已通过模型层修复 |
| 2 | 🟡 | `Math.random()` 用于交易号（walletController, orderService, partyService） | 低优先级，建议后续迭代 |
| 3 | 🟡 | `AsyncStorage` Token非加密存储 | P2，需引入react-native-keychain |
| 4 | 🟢 | Emoji图标占位债务 (95处) | 前端UI优化，不影响功能 |

---

*报告生成时间: 2026-05-05 00:15*
*验证人: Hermes Agent (bug-fix cron)*
