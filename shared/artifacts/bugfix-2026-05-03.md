# Bug修复报告

**日期**: 2026-05-03
**修复数量**: 5个
**审查来源**: code-review-2026-05-03-v5.md
**安全评分**: 90→92

## 已修复

| # | 严重度 | Bug描述 | 修复文件 | 修复详情 |
|---|--------|---------|----------|----------|
| 1 | 🔴 | reset-password无密码用户生产环境可直接重置 | auth.js | 添加 `process.env.NODE_ENV === 'production'` 判断，生产环境无密码用户禁止重置，返回401 |
| 2 | 🟡 | bcrypt salt rounds偏低（10 vs encryption.js的12） | auth.js | register和reset-password的 `bcrypt.hash(..., 10)` 全部改为 `12`，与encryption.js配置一致 |
| 3 | 🟡 | adminAuth未检查Token黑名单 | auth.js (middleware) | adminAuth函数改为async，在JWT验证后添加 `TokenBlacklist.isBlacklisted(token)` 检查 |
| 4 | 🟡 | login路由console.error输出错误详情到客户端 | auth.js | `console.error` → `logger.error`，500响应message改为固定文本 `'Login failed'`，不再拼接 `error.message` |
| 5 | 🟢 | 全局请求体限制10MB过大 | server.js | `express.json({ limit: '10mb' })` 和 `express.urlencoded({ limit: '10mb' })` 均改为 `5mb` |

## 未修复（原因）

| # | 原因 | 计划 |
|---|------|------|
| 1 | mockVerifyCodes Redis迁移 | 开发环境内存存储足够，生产环境部署前迁移 |
| 2 | RN Token存储迁移至Keychain/Keystore | 需要引入 `@react-native-keychain` 依赖，影响APK构建，待单独排期 |
| 3 | tickets.js公开验票接口 /code/:code | 业务必需（线下扫码验票），已加strictLimiter限流防枚举，风险可控 |
| 4 | socialController.js logger统一 | 当前仅2处结构化日志，无敏感信息泄露，低优先级 |

## 验证结果

- ✅ `backend/src/middleware/auth.js` Node.js --check 通过
- ✅ `backend/src/routes/v1/auth.js` Node.js --check 通过
- ✅ `backend/src/server.js` Node.js --check 通过
- ✅ Git commit: backup-auto-20260331-210742 96dd1f8b

## 代码变更统计

```
 backend/src/middleware/auth.js | 15 ++++++++++++++-
 backend/src/routes/v1/auth.js  | 13 ++++++++-----
 backend/src/server.js          |  4 ++--
 4 files changed, 24 insertions(+), 8 deletions(-)
```
